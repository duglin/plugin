Docker `machine` extension

This plugin will invoke Docker's 'machine' executable.
Its just a wrapper for 'machine' so that you can do:
```
     docker machine ...
```

It assumes that `docker-machine` is in your PATH.
